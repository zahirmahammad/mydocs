Simple things make Life Easier

```
uv init ProjectName
cd ProjectName
uv add libraryname
uv remove libraryname
```

on a different machine to install
```
pip install uv
uv sync
```

That's it!! 

This will safely install all the required libraries automatically